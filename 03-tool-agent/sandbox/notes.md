# Sample file the agent may read via the read_file tool.
# Path is relative to this sandbox folder, e.g. notes.md

Week 3 sandbox
==============
The calculator tool only evaluates arithmetic (+ - * / ** %).
The now tool returns the local timezone ISO timestamp.
The read_file tool can only open files in this sandbox directory.
The model never runs these functions itself — our Python does.
